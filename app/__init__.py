# Package initializer for the `app` package.
# This file is intentionally minimal. It exists so Python treats `app` as a package
# and absolute imports like `from app.config import ...` work when running from
# the repository root.
